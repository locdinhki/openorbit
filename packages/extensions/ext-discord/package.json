{
  "name": "@openorbit/ext-discord",
  "version": "1.0.0",
  "description": "Discord bot gateway for remote AI-powered job search control via discord.js",
  "type": "module",
  "main": "./src/main/index.ts",
  "keywords": ["openorbit-extension"],
  "openorbit": {
    "id": "ext-discord",
    "displayName": "Discord Bot",
    "category": "messaging",
    "icon": "message-circle",
    "activationEvents": ["onStartup"],
    "main": "./src/main/index.ts",
    "renderer": "./src/renderer/index.ts",
    "contributes": {
      "settings": [
        { "key": "discord.bot-token", "label": "Bot Token", "type": "password", "description": "Discord bot token for authentication" },
        { "key": "discord.authorized-user-ids", "label": "Authorized User IDs", "type": "string", "description": "Comma-separated Discord user IDs allowed to interact" },
        { "key": "voice.openai-api-key", "label": "Voice API Key (OpenAI)", "type": "password", "description": "Optional fallback — not needed if Whisper CLI is installed locally (pip install openai-whisper)" }
      ]
    }
  },
  "dependencies": {
    "@openorbit/core": "*",
    "@openorbit/ext-jobs": "*",
    "discord.js": "^14.18.0"
  }
}
